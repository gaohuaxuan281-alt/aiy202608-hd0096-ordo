import { FeatureScaffold } from "../shared/FeatureScaffold";

export function TimelinePage() {
  return <FeatureScaffold eyebrow="PLAN & SCHEDULE" title="Timeline" description="按时间呈现考前复习节奏、任务调整与关键节点。" glyph="◫" modulePath="features/timeline" handoff="在这里实现日历、时间轴、拖拽排期和动态调整记录；不要修改其他业务模块。" />;
}
